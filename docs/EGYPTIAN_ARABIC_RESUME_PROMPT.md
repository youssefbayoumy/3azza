# Resume prompt

Continue the Egyptian Arabic (`ar-EG`) implementation in `C:\Users\youss\Desktop\Vibe coding\3azza2\app` through phases 1–8. Read `docs/EGYPTIAN_ARABIC_HANDOFF.md` and `docs/EGYPTIAN_ARABIC_COMPLETION_CHECKLIST.md` completely before editing.

Work autonomously and do not stop after planning or a small batch. First inspect the dirty worktree and preserve unrelated icon/app-metadata changes. Consolidate the transitional localization code into typed, namespaced English and Egyptian-Arabic resources with exact key parity. Then migrate every app-owned user-visible string across screens, shared components, alerts, validation, placeholders, accessibility labels, notifications, errors, empty/loading states, maintenance presentation, and formatting. Use stable maintenance IDs for technical labels. Preserve original manual-source English and citations; show a localized “original manual text is in English” notice until reviewed Arabic exists rather than silently translating it.

Complete RTL behavior and centralized locale formatting, retaining Western digits for odometer/manual compatibility. Add automated tests for key parity, empty/missing translations, interpolation/plurals, persistence, notifications, formatting, and a hard-coded user-visible English scanner with a documented allowlist. Run `npm run typecheck`, `npm run lint`, `npm test`, and `git diff --check`. Do not claim completion while any normal Arabic workflow contains unintended English or any acceptance gate in the handoff remains unmet. Do not build/upload to Play Console; phases 9+ are out of scope unless separately requested.
