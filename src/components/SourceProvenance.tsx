/**
 * Kept as a no-op compatibility seam for older screens and extensions.
 * Audit evidence remains in the internal knowledge data and is intentionally
 * unavailable to production presentation components.
 */
export default function SourceProvenance(): null {
  return null;
}
